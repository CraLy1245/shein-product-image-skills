# Quickstart

1. Clone or download this repository.
2. Copy `shein-adaptive-product-image-set/` into your Codex skills folder.
3. Check `SKILL.md` shows version `1.3.4`.
4. Run `python scripts/validate_skill.py .` inside the skill folder.
5. Use the skill for SHEIN product image set planning.
