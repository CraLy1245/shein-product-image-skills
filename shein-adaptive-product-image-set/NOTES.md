# Notes

This Skill is built for SHEIN-style e-commerce product image set planning and ImageGen execution.

Do not use it as a generic image-generation prompt pack.

Main operating principle:

```text
Analyze the product first → choose necessary modules → write one prompt per image → generate one image at a time → apply v1.3.4 wait/retry rules.
```
