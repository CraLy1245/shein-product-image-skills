# Skill Usage

Ask Codex:

```text
Use $shein-adaptive-product-image-set to analyze this 1688 product package, choose necessary modules, create the planning table, write ImageGen prompts, and generate images one by one only after the plan is confirmed.
```

For planning only:

```text
Use the skill but do not call ImageGen yet. Output only analysis, module selection, Visual Anchor, image planning table, and prompts.
```
