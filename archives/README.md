# Archives

The GitHub connector used for this update can write UTF-8 text files, but it cannot upload binary ZIP assets directly.

For the full downloadable ZIP package, use the local package generated in the ChatGPT workspace:

```text
shein-adaptive-product-image-set-openai-compliant-v1.3.4-mainline.zip
```

The repository contains the extracted Skill folder as normal text files, which is the recommended format for Codex Skill inspection and GitHub version control.
